# Custom-DNS-Server-Implementation-using-UDP-with-Recursive-Query-Forwardin
This project implements a lightweight DNS server using UDP that resolves predefined domain names and supports recursive query forwarding. It parses DNS requests, applies resolution logic, and forwards unresolved queries to an upstream DNS server, ensuring efficient and reliable domain resolution.
